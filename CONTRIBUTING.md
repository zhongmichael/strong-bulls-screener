# 协作开发指南（CONTRIBUTING）

本项目有一套**历史踩坑总结出来的硬规则**。改动相关代码前请务必过一遍本文，否则很容易引入难以察觉的数据错误（这类 bug 通常表现为「数字看着正常但其实是错的」，比直接报错危险得多）。

---

## 1️⃣ 最重要的规则：评分逻辑有三份拷贝，必须同步修改

回踩评分（`score_pullback` / `scorePullback`）等核心算法在**三处**各有一份实现：

| 位置 | 用途 | 说明 |
|---|---|---|
| `compute_strong.py` | 离线全量计算 | 每日构建的主路径 |
| `dist_strong/refresh_core.js` | 服务端增量刷新 | `/api/refresh` 调用，只重算最近若干日 |
| `strong_screener.html`（根目录） | 前端即时计算 | **根目录是源**，构建脚本同步到 `dist_strong/` |

**规则**：任何算法改动必须三处同时改，且**用同一批股票对拍 Python 与 JS 结果，必须完全一致**后才能提交。

V2.1 买点同理（`compute_buydian_v21.py` ↔ `refresh_core.js` 的 `bd21ComputeAll`）。

---

## 2️⃣ 三套数据源与页面的对应关系

只验证一套数据源就发布会漏掉错误。三套互相独立：

| 数据源 | 页面用途 |
|---|---|
| `strong_data.js`（meta） | 主表 / 强势池（收盘价、当日涨跌幅） |
| `kline_ref.js` | 多周期判断、回踩评分（日K + 周/月收盘） |
| `year_kline.js`（`[date, close]`） | 涨幅榜、周期收益、趋势分类、**涨跌停家数与温度分** |

**改刷新逻辑时必须逐一验证这三条链路**。

---

## 3️⃣ 已知陷阱（已修复，请勿回退）

### 3.1 盘中快照固化 ⚠️ 最易复发

收盘后立即刷新时，腾讯 K 线镜像返回的"当日 bar"可能是**盘中快照**（价格/成交量停在盘中某刻，2026-09-04 曾影响约 1700 只票，2026-09-11 达 83% 的票）。

**特征**：`open` 正确、`close/low/vol` 错误 → 所有用当日收盘价算的指标全部错（涨跌停家数、温度分、回踩评分、买点）。

**现有防护**（`refresh_core.js`）：
- 对接口返回的最近 N 根逐根校验，内容不同则覆盖；
- **open 必须一致**（区分快照与复权差异）；
- **成交量不得倒退**（成交量单调递增，防止用快照覆盖正确的收盘数据）；
- `server.js` 记录最近一次真刷时间，**收盘后首次访问强制真刷一次**核对。

**注意**：早退分支 `if(!added && !filled && !changed) return` 会吞掉修复型刷新，新增的修复计数（`ykUpdated`、`fixed`）必须一并纳入「有无变化」判断。

### 3.2 补当日 bar 不能用覆盖率阈值做开关

腾讯接口每天稳定漏同步 0.3%~1% 的票，覆盖率常 >99%，阈值永不触发。正确判据是「**交易日 + 存在滞后股**」。

### 3.3 周/月收盘与 year_kline 必须取 `ref[sym].d` 的真实最后一根

不能用接口返回的最后一根（补 bar 后两者差一天），否则涨幅榜当日涨跌幅会显示 `--`。

### 3.4 涨跌幅缓存写入前必须比对日期

末日期 ≠ 全局最新日的置 `null`，页面显示 `--`。否则停牌股会把上一交易日的涨幅冒充最新日——**静默误导，比 `--` 更危险**。

### 3.5 排查手法

用 `qt.gtimg.cn` 的 `vol` 区分**停牌**（`vol<=0`，显示 `--` 正确）与**接口漏同步**（`vol>0`，是 bug）。

---

## 4️⃣ 涨停判定口径（已定稿，勿回退）

### 双轨制

- **温度分输入**：沿用上线以来的宽松口径（涨幅≥涨停幅-2%、ST 恒 5%、映射 15/105、1/39、2/6）。已与历史曲线对拍 0 差异，**永久冻结不改**。
- **卡片/图表展示**：涨跌停家数、连板高度用**真实涨跌停价**判定（`round(前收×(1±幅度))` 到分）。代码中展示读 `s.zt/s.dt/s.maxB`，温度分读 `s.ztL/s.dtL/s.maxBL`。

### ST 涨跌幅新规

2026-07-06 起沪深主板 ST/\*ST 由 5% 调为 10%；创业/科创板 ST 维持 20%。`ztLimit(code, name, date)` 按日期分界逐 bar 传当日日期。

### 标的范围

数据源只有沪深 A 股（约 5200 只，含 ST），**没有北交所、没有 B 股**。因此涨跌停家数与同花顺等行情软件存在约 350 只固定差异（多出的几乎全是北交所股票），属正常口径差，**不是 bug**。

---

## 5️⃣ 发布流程

### 发布前必做

**先比对本地 `dist_strong` 与线上的 `dates` 末日**。服务端每天自增量刷新，本地通常落后一天，直接整包发布会**回滚线上数据**。

正确顺序：

```bash
# 1. 备份线上数据包与代码
# 2. 拉线上数据包覆盖本地 dist_strong（补齐线上比本地新的部分）
# 3. 跑 bash update_all.sh 刷新
# 4. python3 rebuild_gz.py 重建 .gz
# 5. 自检 .js 与 .gz 内容一致
# 6. 发布
```

⚠️ 拉线上包做对照的临时文件（`*.online.js`）**务必在发布前从 `dist_strong` 删除**，否则会被整包上传（2026-09-05 曾误传 143MB）。本仓库 `.gitignore` 已排除该模式。

### 发布铁律

**必须以 `language=node` 发布，绝不能 `language=static`** —— 静态发布会覆盖线上 node 服务，`/api/refresh` 随之失效、页面「刷新最新数据」按钮报错。

启动参数：`startCmd = node server.js`，`PORT` 由平台注入（本地默认 3000）。

---

## 6️⃣ 提交规范

- 分支：`feature/xxx`、`fix/xxx`
- 提交信息用前缀：`feat:` / `fix:` / `data:` / `docs:` / `refactor:`
- **不要提交数据文件**（`.gitignore` 已排除，见 README「数据文件」一表）
- 涉及算法改动的 PR，请附上**对拍结果**（Python 与 JS 在同一批股票上的输出一致性）
- 涉及数据修复的 PR，请附上**修复前后的数值对比**与验证口径

---

## 7️⃣ 待办方向

- 北交所行情接入（可使涨跌停家数与行情软件完全一致，需新增数据源并改刷新/合并逻辑）
- 服务端与 Python 算法去重（当前三份拷贝靠人工同步，长期看易出错）
